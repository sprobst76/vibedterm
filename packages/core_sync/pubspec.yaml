name: core_sync
description: Server sync and authentication for VibedTerm vault synchronization.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  meta: ^1.11.0
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
